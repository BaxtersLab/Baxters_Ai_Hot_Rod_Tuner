# TODO — Baxters AI Hot Rod Tuner

1. Create `src/hotrod_tuner/` Python package scaffold with `__init__.py` and a minimal `app.py` FastAPI stub.
2. Add `requirements.txt` listing `fastapi`, `uvicorn`, and `pydantic`.
3. Add `tests/test_policy.py` with token-bucket and hysteresis unit tests.
4. Add example telemetry payloads in `examples/`.
5. Add Glue adapter contract notes for `governor` capabilities.

Priority: start with the API scaffold + unit tests for policy logic.
